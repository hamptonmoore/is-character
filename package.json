{
  "name": "is-character",
  "version": "1.0.0",
  "description": "Check if a string is a single character",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Hampton Moore",
  "license": "MIT",
  "dependencies": {
    "is-string": "^1.0.5"
  }
}
